

# Убрать взаимоисключение причин и комментария

Сейчас в `CancellationReasonDialog` ввод комментария очищает выбранные причины (строки 113-115). Нужно убрать эту логику, чтобы пользователь мог одновременно выбрать причины из списка И написать свой комментарий.

## Изменение

**Файл: `src/components/CancellationReasonDialog.tsx`**

1. Убрать строки 113-115 — условие `if (e.target.value.length > 0) { setSelectedReasons([]); }` в onChange textarea.
2. Обновить placeholder на "Дополнительный комментарий" / "Қосымша түсініктеме" чтобы было понятно что комментарий — дополнение к причинам.

Одно изменение в одном файле.

