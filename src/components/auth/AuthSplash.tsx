import { AuthMark } from "@/components/auth/AuthMark";

const AuthSplash = () => (
  <div className="min-h-screen flex items-center justify-center bg-white">
    <AuthMark variant="brand" className="h-9 w-auto" />
  </div>
);

export default AuthSplash;
